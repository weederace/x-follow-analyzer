# ساخت اپ اندروید (APK)

نسخهٔ اندروید همان فایل‌های پوشهٔ `web/` است که داخل یک WebView بومی اجرا می‌شوند —
بدون هیچ باندلر و بدون هیچ مرحلهٔ build میانی. پس چیزی که روی دسکتاپ تست می‌کنید
بایت‌به‌بایت همان چیزی است که در APK اجرا می‌شود.

مهم‌تر از همه: **اپ اندروید کاملاً آفلاین است.** آرشیو ZIP در خود گوشی خوانده
می‌شود (`analyzer.js`)، تاریخچهٔ بررسی‌شده‌ها در حافظهٔ خود اپ می‌ماند
(`history.js`)، و اپ هیچ درخواست شبکه‌ای از خودش نمی‌فرستد. مجوز اینترنت هم لازم
نیست جز برای باز کردن پروفایل در مرورگر یا اپ X.

---

## راه سریع: یک دستور

اگر ویندوز دارید، لازم نیست هیچ‌کدام از بخش‌های بعدی را بخوانید. این را در ریشهٔ
پروژه اجرا کنید:

```bat
powershell -ExecutionPolicy Bypass -File build-apk.ps1
```

اسکریپت همان کارهایی را می‌کند که در بخش‌های بعدی دستی توضیح داده شده: Node و
وابستگی‌ها را بررسی می‌کند، یک JDK مناسب پیدا می‌کند، Android SDK را پیدا می‌کند
(و اگر نبود، با اجازهٔ خودتان فقط ابزارهای خط فرمانش را نصب می‌کند — حدود ۵۰۰
مگابایت با پلتفرم و build-tools، مقابل چند گیگابایت کل Android Studio)،
`local.properties` را می‌نویسد، آیکون‌ها را سر جایشان می‌گذارد، build می‌گیرد و
فایل نهایی را اینجا می‌گذارد:

```
dist\follow-desk-debug.apk
```

سه سوئیچ دارد:

| سوئیچ | کار |
|---|---|
| `-InstallSdk` | بدون پرسیدن، SDK را نصب کن (برای اجرای بدون نظارت) |
| `-SdkDir PATH` | SDK را کجا نصب کند — پیش‌فرض `C:\Android\Sdk` |
| `-JavaHome PATH` | از این JDK استفاده کن، به‌جای جست‌وجو |

مثال، وقتی JDK جای غیرمعمولی است:

```bat
powershell -ExecutionPolicy Bypass -File build-apk.ps1 -JavaHome "D:\jdk-17"
```

بیرون از پوشهٔ پروژه فقط دو جا چیزی می‌نویسد، و فقط اگر خودش SDK نصب کرده باشد:
پوشهٔ SDK، و متغیر `ANDROID_HOME` در سطح حساب کاربری شما. اگر SDK را فقط *پیدا*
کرده باشد، هیچ متغیری را دست نمی‌زند — تنظیم دائمی چیزی روی حساب کسی، وقتی
لازم نیست، تغییری است که نخواسته‌اید و خبر هم از آن ندارید.

پیام‌هایش انگلیسی است، برخلاف بقیهٔ مستندات این پروژه. دلیلش این است که
PowerShell 5.1 ویندوز، فایل `.ps1` بدون BOM را با کدپیج محلی می‌خواند و متن
فارسی در آن به هم می‌ریزد؛ لاگی که خوانده نشود از لاگ به زبان غلط بدتر است. هر
خطایی که این اسکریپت می‌تواند بدهد، در همین فایل بخش متناظر دارد.

روی لینوکس و مک اسکریپت معادلی نیست؛ همان مسیر دستی بخش بعد را برو، با
`npm run android:apk:unix` در قدم آخر.

---

## چه چیزی لازم دارید (یک‌بار)

این ابزارها روی همان کامپیوتری که build می‌کنید نصب می‌شوند، نه روی گوشی:

| ابزار | نسخه | از کجا |
|---|---|---|
| Node.js | ۲۰ یا بالاتر | <https://nodejs.org> |
| JDK | ۱۷ تا ۲۰ | JDK همراه Android Studio (نسخهٔ ۱۷) کافی است |
| Android SDK | پلتفرم ۳۴ و build-tools ۳۴ | `build-apk.ps1` خودش نصب می‌کند، یا Android Studio |

Android Studio لازم *نیست* — فقط اگر بخواهید لاگ‌ها را ببینید یا روی شبیه‌ساز
اجرا کنید به کار می‌آید. برای ساختن APK، ابزارهای خط فرمان کافی‌اند.

بازهٔ JDK اتفاقی نیست: پروژه‌ای که Capacitor تولید می‌کند Gradle 8.2.1 و
AGP 8.2.1 دارد؛ AGP کمتر از ۱۷ را نمی‌پذیرد و این نسخهٔ Gradle بالاتر از ۲۰ را.
بیرون از این بازه با پیام `Unsupported class file major version` شکست می‌خورد.

اگر دستی نصب می‌کنید، در SDK Manager این دو را انتخاب کنید: «Android SDK
Platform 34» و «Android SDK Build-Tools»، و بعد `ANDROID_HOME` را روی پوشهٔ SDK
تنظیم کنید (معمولاً `C:\Users\<نام‌شما>\AppData\Local\Android\Sdk`).

---

## ساخت APK دستی

در ریشهٔ پروژه، یک‌بار:

```bat
npm install
npm run android:init
```

`android:init` پوشهٔ `android/` را می‌سازد. این پوشه در `.gitignore` هست چون
حدود ۲۰۰ مگابایت خروجی تولیدشدهٔ Gradle است — هر وقت لازم شد با همین دستور
دوباره ساخته می‌شود.

و چون دوباره ساخته می‌شود، هر چیزی که *مال ما* است و در `android/` جا دارد باید
جای دیگری نگه داشته شود و بعد کپی شود. آن چیز آیکون‌ها و صفحهٔ شروع‌اند و در
`android-res/` هستند؛ `tools/apply-android-res.mjs` آن‌ها را سر جایشان می‌گذارد و
به هر سه اسکریپت `android:init`، `android:sync` و `android:apk` وصل است، پس
لازم نیست خودتان صدایش بزنید. بدون این قدم، اپ با لوگوی پیش‌فرض Capacitor روی یک
مربع سفید نصب می‌شود — چیزی که تا وقتی APK روی گوشی نباشد معلوم نمی‌شود.
(اگر `web/icon.svg` را عوض کردید، با `python tools/make_android_icons.py` آیکون‌ها
را از نو بسازید.)

بعد، هر بار که چیزی در `web/` تغییر داد:

```bat
npm run android:apk
```

روی لینوکس یا مک به‌جای آن `npm run android:apk:unix`.

فایل خروجی اینجاست:

```
android\app\build\outputs\apk\debug\app-debug.apk
```

اگر این دستور با `SDK location not found` شکست خورد — که روی کامپیوتری که تا حالا
اندروید روی آن build نشده حالت معمول است — بخش «اگر build شکست خورد» پایین‌تر
راه‌حل دستی‌اش را دارد، یا `build-apk.ps1` همان را خودکار انجام می‌دهد.

این فایل را روی گوشی کپی کنید و بازش کنید. اندروید می‌پرسد که نصب از منبع
ناشناس را اجازه می‌دهید یا نه — چون این APK با کلید debug امضا شده و از
Play Store نیامده. برای استفادهٔ خودتان کافی است.

اگر خواستید داخل Android Studio کار کنید (برای دیدن لاگ‌ها یا اجرا روی
شبیه‌ساز):

```bat
npm run android:open
```

---

## آرشیو X را چطور روی گوشی بیاورید

آرشیو را باید از سایت X درخواست کنید — روی گوشی هم می‌شود، ولی راحت‌تر است
روی کامپیوتر بگیرید و بعد منتقل کنید:

1. در X: Settings → Your account → Download an archive of your data
2. X یک ایمیل با لینک دانلود می‌فرستد (معمولاً ۲۴ ساعت بعد)
3. فایل ZIP را دانلود کنید — **بازش نکنید و از حالت ZIP درنیاورید**
4. ZIP را به گوشی منتقل کنید: با کابل USB، یا Telegram Saved Messages، یا
   Google Drive، یا هر روشی که راحت‌تر است
5. در اپ روی «انتخاب فایل آرشیو» بزنید و همان ZIP را انتخاب کنید

فایل ZIP آرشیو معمولاً بین ۵۰ مگابایت تا چند گیگابایت است (بستگی به تعداد
عکس‌ها و ویدیوهایتان دارد). اپ از داخلش فقط فهرست فالوور و فالویینگ و فایل
`account.js` را باز می‌کند و بقیه را حتی از حالت فشرده درنمی‌آورد — پس حتی
آرشیوهای بزرگ هم سریع پردازش می‌شوند.

---

## تفاوت‌های نسخهٔ گوشی

چند چیز روی گوشی متفاوت است، چون روی موبایل معنا نمی‌دهند:

**باز کردن گروهی چند پروفایل.** روی گوشی `window.open` آدرس را به سیستم تحویل
می‌دهد، پس ۱۰ لینک یعنی ۱۰ بار پرج بین اپ‌ها و گم شدن صف زیر انبوهی از
صفحه‌های X. به‌جای آن، دکمهٔ «باز کردن N تای بعدی» روی خودِ کارت است — همان‌جا که
صف است — و تعدادش را از ردیف زیر کارت انتخاب می‌کنی: ۵، ۱۰، ۲۰ یا ۵۰. آن انتخاب
روی گوشی حذف نشده، چون گوشی دقیقاً همان جایی است که یک‌باره باز شدن پنجاه تب را
بیشتر از همه حس می‌کنی.

**انتخاب فایل آرشیو.** هیچ پلاگینی برای این کار نصب نیست و هیچ مجوزی هم لازم
نیست: همان `<input type="file">` داخل WebView، انتخابگر سند خود اندروید (SAF) را
باز می‌کند و فایل را دست اپ می‌دهد. تنها تفاوت با نسخهٔ وب این است که روی گوشی
صفت `accept` از input برداشته می‌شود. دلیلش یک باگ واقعی بود: SAF فیلتر را روی
نوع MIME می‌زند، و ZIP آرشیو بسته به اینکه از Downloads یا Drive یا Telegram
بیاید `application/zip` یا `application/x-zip-compressed` یا فقط
`application/octet-stream` معرفی می‌شود. نتیجه این بود که فایل درست، در فهرست
خاکستری و غیرقابل‌انتخاب می‌ماند — بدون هیچ پیام خطایی، فقط یک فایل که کلیک
نمی‌خورد. فیلتر برداشته شد و اعتبارسنجی به `analyzer.js` سپرده شد، که هر طور
باشد باید ZIP را باز کند و اگر نتوانست پیام قابل‌فهم بدهد.

**یادآوری آرشیو قبلی.** اپ نام و اندازهٔ آخرین آرشیوی که خواندید را نگه می‌دارد
تا کارت شروع بتواند بگوید «قبلاً `twitter-2026-...zip` را خوانده‌ای». فقط همین
چهار چیز ذخیره می‌شود — نام، اندازه، تاریخ فایل، و زمان خواندن — و هیچ‌چیزی از
فهرست فالوها. مرورگر و WebView اجازه نمی‌دهند فایل را از روی مسیرش دوباره باز
کنیم، پس این یادآوری است نه میان‌بر؛ فایل را باید دوباره انتخاب کنید.

**نوار پیشرفت خواندن.** آرشیو چند گیگابایتی روی گوشی چند ده ثانیه طول می‌کشد و
جای سه نقطهٔ چشمک‌زن نیست. عددهای نوار از خود خواننده می‌آید: اول اسکن فهرست
مرکزی ZIP، بعد باز کردن همان چند فایلی که لازم است. دو کار متفاوت‌اند، پس هر
کدام سهم خودش را از یک نوار دارد (`DIRECTORY_SHARE` در `analyzer.js`) — وگرنه
نوار یک‌بار پر می‌شد و از صفر شروع می‌کرد، که شبیه گیر کردن است.

**میان‌برهای کیبورد.** راهنمای کلیدها نمایش داده نمی‌شود چون کیبوردی نیست.
(اگر کیبورد بلوتوث وصل کنید کلیدها هنوز کار می‌کنند، فقط راهنمایشان پیدا نیست.)

**تاریخچهٔ مشترک با دسکتاپ.** روی گوشی تاریخچه در حافظهٔ خود اپ می‌ماند و با
نسخهٔ دسکتاپ یکی نمی‌شود. اگر روی کامپیوتر ۲۰۰ نفر را بررسی کرده‌اید، روی
گوشی از صفر شروع می‌کنید. (همگام‌سازی یعنی فرستادن داده به یک سرور، و کل
منطق این پروژه این است که هیچ‌چیز از دستگاه شما بیرون نرود.)

**رابط برای انگشت.** همهٔ دکمه‌ها و ردیف‌های فهرست حداقل ۴۴dp هدف لمس دارند،
صفحهٔ شروع و بدنه safe-area گوشی‌های بدون حاشیه را رعایت می‌کنند، و صفحهٔ شروع
شب با تم تاریک هماهنگ است.

---

## آیکون و صفحهٔ شروع

آیکون لانچر از `web/icon.svg` می‌آید — همان علامتی که در نسخهٔ وب بالای صفحه است —
و رنگ‌هایش از `--desk`، `--card` و `--stamp` در `web/app.css` خوانده می‌شوند، تا
آیکون و رابط نتوانند از هم دور شوند.

`python tools/make_android_icons.py` هفده فایل در `android-res/` می‌سازد، به‌علاوهٔ
دو فایل استایل شب که دستی نگه داشته می‌شوند:

- `mipmap-*/ic_launcher.png` در پنج تراکم (۴۸dp) — آیکون کلاسیک، برای اندروید زیر ۸
- `mipmap-*/ic_launcher_round.png` — همان، بریده به دایره
- `mipmap-*/ic_launcher_foreground.png` (۱۰۸dp) — لایهٔ جلویی آیکون adaptive
- `values/ic_launcher_background.xml` — لایهٔ پشتی، رنگ `--desk`
- `drawable/splash.xml` — صفحهٔ شروع
- `values-night/styles.xml` و `drawable-night/splash.xml` — صفحهٔ شروع در تم تاریک

آیکون adaptive روی ۱۰۸dp کشیده می‌شود ولی لانچر فقط دایرهٔ میانی ۷۲dp را تضمین
می‌کند و بسته به گوشی، شکل ماسک متفاوت است؛ پس علامت داخل حاشیهٔ امن ۶۶dp نگه
داشته شده تا هیچ لانچری آن را نبُرد. (`tests/test_android.py` همین را هم بررسی
می‌کند: هر پیکسل در حاشیه باید شفاف باشد.)

صفحهٔ شروع یک `layer-list` است، نه بیتمپ: رنگ میز، با علامت اپ در وسط. Capacitor
یازده `splash.png` می‌گذارد که هر کدام کشیده می‌شوند تا کل صفحه را پر کنند و هر
کدام لوگوی قالب خودش را دارد؛ `apply-android-res.mjs` آن یازده فایل را پاک
می‌کند. پاک کردن لازم است و فقط جایگزین گذاشتن کافی نیست، چون اندروید
`drawable/splash.xml` و `drawable-port-mdpi/splash.png` را دو حالت از *یک* منبع
می‌بیند و روی گوشی‌ای که با آن تراکم بخواند، بیتمپ برنده می‌شود.

رنگ صفحهٔ شروع همان رنگی است که WebView در اولین فریم می‌کشد
(`backgroundColor` در `capacitor.config.json`)، پس لحظهٔ جابه‌جایی دیده نمی‌شود؛
اپ باز می‌شود بدون آن پرش سفیدی که اپ‌های WebView معمولاً دارند.

همهٔ این‌ها در `tests/test_android.py` تست دارند — اندازهٔ هر پانزده تصویر، رنگ
پشت آیکون، دایره بودن آیکون گرد، خالی بودن حاشیهٔ امن، سالم بودن XML‌ها،
کپی‌شدن منابع شب، و اینکه هر چهار اسکریپت npm زنجیرهٔ کپی را دارند. دلیلش
این است که این دسته از خرابی‌ها بی‌صدا هستند: build موفق می‌شود، هیچ خطایی
نیست، و فقط وقتی APK روی گوشی است معلوم می‌شود که آیکونش لوگوی Capacitor است.

---

## نام اپ، و رنگ‌هایی که قبل از CSS کشیده می‌شوند

این اپ دو راه نصب دارد و هر کدام نامش را از جای دیگری برمی‌دارد. APK نامش را از
`appName` در `capacitor.config.json` می‌گیرد (که Capacitor آن را در
`strings.xml` می‌نویسد)، و نصب از مرورگر — همان «Add to Home screen» وقتی
`x_analyzer_server.py` را اجرا کرده‌اید — از `short_name` در
`web/manifest.webmanifest`. این دو باید یک رشته باشند، وگرنه یک اپ روی دو گوشی
دو اسم دارد؛ `tests/test_android.py` همین را بررسی می‌کند.

و به همین دلیل، `manifest.webmanifest` با اینکه رابط اپ فارسی باز می‌شود،
`"lang": "en"` و `"dir": "ltr"` دارد. این دو صفت، زبان و جهت *رشته‌های خود
manifest* را اعلام می‌کنند — یعنی `name` و `short_name` و `description` — نه زبان
رابط. آن رشته‌ها نام اپ‌اند و در هر دو مسیر نصب انگلیسی‌اند، پس همین درست است.
قبلاً `fa` و `rtl` نوشته بود در حالی که رشته‌ها انگلیسی بودند، که یعنی به مرورگر
می‌گفتیم متن لاتین را راست‌به‌چپ بچین. زبان رابط چیز دیگری است: انتخاب زمان اجرا
است، در `localStorage` می‌ماند، و یک فایل ثابت نمی‌تواند دنبالش برود —
`document.title` دنبالش می‌رود و `tests/test_frontend.mjs` هم همین را از آن
می‌خواهد.

رنگ میز (`--desk` در `web/app.css`) در سه جای دیگر هم کپی شده، چون هر سه توسط
چیزی خوانده می‌شوند که *قبل از* app.css اجرا می‌شود: `theme_color` و
`background_color` در manifest (پیام نصب مرورگر)، دو `<meta name="theme-color">`
در `index.html` (نوار وضعیت اندروید، یکی برای روز و یکی برای شب)، و
`backgroundColor` در `capacitor.config.json` (اولین فریم WebView). هیچ‌کدام
نمی‌توانند یک custom property را ارجاع بدهند، پس تنها کاری که می‌شود کرد این است
که نگذاریم از استایل‌شیت جدا بیفتند: `tests/test_android.py` رنگ را از
`web/app.css` می‌خواند — از بلوک `:root` برای روز و از `[data-theme="dark"]`
برای شب — و هر سه فایل را با آن می‌سنجد. manifest چون media query ندارد فقط
می‌تواند یک رنگ داشته باشد و آن رنگ روز است؛ کار شب را همان دو meta می‌کنند.

---

## اگر build شکست خورد

هر سه مشکل زیر را `build-apk.ps1` خودکار حل می‌کند؛ این بخش برای وقتی است که
دستی build می‌گیرید، یا روی لینوکس و مک هستید، یا می‌خواهید بدانید اسکریپت
دقیقاً چه کاری می‌کند.

### `SDK location not found`

پیام کامل چیزی شبیه این است:

```
> SDK location not found. Define a valid SDK location with an ANDROID_HOME
  environment variable or by setting the sdk.dir path in your project's
  local properties file at 'D:\...\android\local.properties'.
```

معنایش این است که Node و Gradle و JDK سالم‌اند و کار تا آخرین قدم پیش رفته؛
فقط Gradle نمی‌داند Android SDK کجاست. دو حالت دارد. (هر دو را
`build-apk.ps1` خودکار حل می‌کند؛ پایین برای وقتی است که می‌خواهید دستی جلو بروید
یا ویندوز ندارید.)

**حالت اول: SDK نصب است ولی `ANDROID_HOME` تنظیم نشده.** این دستور را در
PowerShell و در ریشهٔ پروژه اجرا کنید. خودش SDK را پیدا می‌کند و مسیرش را
می‌نویسد:

```powershell
$sdk = @("$env:ANDROID_HOME", "$env:ANDROID_SDK_ROOT",
         "$env:LOCALAPPDATA\Android\Sdk", "C:\Android\Sdk") |
       Where-Object { $_ -and (Test-Path "$_\platform-tools") } |
       Select-Object -First 1
if ($sdk) {
  # جاوا فایل‌های .properties را ISO-8859-1 می‌خواند، پس هر حرف غیرASCII باید
  # \uXXXX بشود — وگرنه یوزرنیم فارسی به «?» تبدیل می‌شود و Gradle باز هم
  # می‌گوید SDK را پیدا نکردم.
  $out = New-Object System.Text.StringBuilder
  foreach ($ch in ($sdk -replace '\\','/').ToCharArray()) {
    if ([int] $ch -gt 126) { [void] $out.Append(('\u{0:x4}' -f [int] $ch)) }
    else { [void] $out.Append($ch) }
  }
  "sdk.dir=$($out.ToString())" | Set-Content -Encoding ASCII android\local.properties
  "پیدا شد: $sdk  — حالا دوباره npm run android:apk را بزنید"
} else {
  "SDK نصب نیست. سراغ حالت دوم بروید."
}
```

`local.properties` در `.gitignore` هست، چون مسیر دیسک شخصی شما را نگه می‌دارد
و روی کامپیوتر کسی دیگر بی‌معنا است.

یک نکتهٔ ریز: اگر مسیر SDK حرف غیرانگلیسی دارد (مثلاً نام کاربری فارسی)، خط بالا
کار نمی‌کند. جاوا فایل‌های `.properties` را ISO-8859-1 می‌خواند، پس آن حرف‌ها باید
به شکل `\uXXXX` نوشته شوند وگرنه علامت سؤال می‌شوند و Gradle دوباره همین خطا را
می‌دهد. `build-apk.ps1` این تبدیل را انجام می‌دهد؛ در حالت دستی، SDK را جایی با
مسیر انگلیسی بگذارید (مثل `C:\Android\Sdk`).

**حالت دوم: SDK نصب نیست.** لازم نیست کل Android Studio (چند گیگابایت) را نصب
کنید؛ اگر فقط APK می‌خواهید و به IDE کاری ندارید، ابزارهای خط فرمان کافی‌اند —
حدود ۱۵۰ مگابایت. از <https://developer.android.com/studio#command-line-tools-only>
فایل `commandlinetools-win-*.zip` را بگیرید و:

```powershell
mkdir C:\Android\Sdk\cmdline-tools\latest
# محتوای زیپ را داخل همین پوشهٔ latest باز کنید (طوری که bin\sdkmanager.bat دیده شود)
cd C:\Android\Sdk\cmdline-tools\latest\bin
.\sdkmanager.bat "platform-tools" "platforms;android-34" "build-tools;34.0.0"
[Environment]::SetEnvironmentVariable('ANDROID_HOME', 'C:\Android\Sdk', 'User')
```

بعد PowerShell را ببندید و باز کنید (تا متغیر محیطی خوانده شود) و دوباره
`npm run android:apk` را اجرا کنید.

نسخهٔ ۳۴ اتفاقی نیست: پروژهٔ تولیدشده `compileSdk 34` و `targetSdk 34` می‌خواهد
و `minSdk 22` است، یعنی اپ روی اندروید ۵.۱ و بالاتر نصب می‌شود.

### `Unsupported class file major version` یا خطای Kotlin/Gradle

یعنی JDK شما بیرون از بازهٔ ۱۷ تا ۲۰ است — معمولاً جدیدتر. یک JDK 17 نصب کنید
(<https://adoptium.net>) و همان را به Gradle نشان دهید:

```powershell
[Environment]::SetEnvironmentVariable('JAVA_HOME', 'C:\Program Files\Java\jdk-17', 'User')
```

`build-apk.ps1` این را قبل از شروع build بررسی می‌کند و اگر JDK مناسبی پیدا نکند
همان اول می‌گوید، نه بعد از چند دقیقه دانلود Gradle. اگر JDK درست دارید ولی جای
غیرمعمولی است، `-JavaHome` را به آن بدهید.

### اولین build کند است

دفعهٔ اول Gradle خودش را (حدود ۱۵۰ مگابایت) دانلود می‌کند و چند دقیقه طول
می‌کشد — در لاگ `Downloading https://services.gradle.org/...` را می‌بینید. این
یک‌بار است؛ build های بعدی چند ده ثانیه‌اند.

---

## دربارهٔ APK امضاشده

خروجی این پروژه یک APK با کلید debug است، و برای نصب روی گوشی خودتان همین درست
است: نصب می‌شود، کار می‌کند، و تنها هزینه‌اش آن یک‌بار «نصب از منبع ناشناس را
اجازه می‌دهید؟» است.

**اما این پروژه از build امضاشدهٔ release پشتیبانی نمی‌کند، و بهتر است صریح
بگوییم چرا.** `android/app/build.gradle` که Capacitor می‌سازد، قالب دست‌نخوردهٔ
خودش است: هیچ `signingConfigs` ندارد و هیچ‌جایش فایل کلید را نمی‌خواند. یعنی
`assembleRelease` یک APK **بدون امضا** می‌دهد که اندروید نصبش نمی‌کند، ولی Gradle
آن را «موفق» گزارش می‌کند — بدترین حالت ممکن، چون فکر می‌کنید APK دارید و ندارید.
به همین دلیل `build-apk.ps1` سوئیچ `-Release` ندارد؛ داشتنش یعنی وعدهٔ چیزی که
عمل نمی‌شود.

راه درستش این نیست که `android/app/build.gradle` را دستی ویرایش کنید: آن پوشه
gitignore شده و اولین `npm run android:init` بعدی ویرایش شما را پاک می‌کند. برای
اینکه واقعاً کار کند، بلوک امضا باید مثل `android-res/` جایی پیگیری‌شده بماند و
بعد از هر تولید تزریق شود.

اگر روزی لازم شد (یعنی اگر بخواهید اپ را به دیگران بدهید یا در Play Store منتشر
کنید)، مسیرش این است: با `keytool` یک کلید بسازید، مقادیرش را در
`android/keystore.properties` بگذارید، و یک قدم به `tools/apply-android-res.mjs`
اضافه کنید که `signingConfigs` را در `build.gradle` تولیدشده تزریق کند.

```bat
keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```

`*.jks`، `*.keystore` و `keystore.properties` همه در `.gitignore` هستند و
**هرگز نباید commit شوند** — اگر کلید امضا لو برود، هر کسی می‌تواند به نام
شما آپدیت منتشر کند. این سه خط در `.gitignore` از قبل هستند، برای همان روز.
